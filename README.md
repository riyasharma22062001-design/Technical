# Snake Game

Self-contained HTML5 Canvas Snake – **no install needed!**

Open `snake.html` in browser (double-click or `start snake.html`). Arrow keys to play, click to restart after game over.
